# Fork of python-jobspy, pinned by tag: adds LinkedIn applicant counts, whether a
# LinkedIn application is completed offsite, the freehire.me source, salary
# extraction outside the US, and the board fields upstream collects and discards.
# Sources at https://github.com/pa741/JobSpy (branch `patches`).
# A GitHub archive rather than git+https, so the slim image needs no git binary.
https://github.com/pa741/JobSpy/archive/refs/tags/v1.1.82-fh6.tar.gz
pandas
azure-storage-blob
python-dotenv
pyyaml
